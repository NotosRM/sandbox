Архивируй эксперимент: обнови README с финальными выводами.

Параметры: $ARGUMENTS
Ожидаемый формат: `<имя-эксперимента>`
Пример: `zustand-counter`

Шаги:

1. Проверь, что `experiments/<имя>` существует. Если нет — сообщи об ошибке и остановись.
2. Прочитай код, тесты и текущий README эксперимента.
3. Если статус уже не ясен — спроси: `done` (завершён, может быть полезен) или `archived` (устарел/не нужен)?
4. Обнови `experiments/<имя>/README.md`:
   - Измени строку `**Статус:**` на `done` или `archived`.
   - Заполни/обнови секцию "Выводы": что работает, что не понравилось, какой подход выбран и почему.
     Выводы пиши конкретно, опираясь на код и тесты — не "всё хорошо", а "Zustand требует меньше бойлерплейта чем Redux для счётчика, обновление store синхронное".
5. Сохрани файл.
6. Выведи: какие выводы зафиксированы.
