import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('module loads', () => {
    // Placeholder: @testing-library/vue added when first Vue experiment needs it
    expect(true).toBe(true);
  });
});
